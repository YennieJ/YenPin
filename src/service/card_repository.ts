import { firebaseDatabase } from "./firebase";
import { getDatabase, ref, set } from "firebase/database";

// class CardRepasitory{
//   saveCard(card:any) {
//     const db=getDatabase()
//     db.ref(/cards/).set(card);
//   }

// }
